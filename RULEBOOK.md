# FantasyR Rulebook

이 문서는 이 저장소를 수정하는 사람과 코딩 AI가 따라야 할 게임 UI 기준이다.
새 게임을 추가하거나 기존 게임의 시작 화면을 수정할 때 반드시 먼저 읽는다.

## 기본 원칙

- 판타지왕국 시작 화면을 전체 게임의 공통 구조 기준으로 사용한다.
- 판타지왕국의 갈색과 금색을 다른 게임에 복사하지 않는다.
- 공통화 대상은 레이아웃, 조작 방식, 간격, 컴포넌트 크기와 화면 흐름이다.
- 게임별 개성은 로고, 배경, 강조색, 이미지와 게임 전용 설정으로 표현한다.
- 모든 게임에 싱글플레이와 멀티플레이가 존재하는 것을 기준으로 설계한다.
- 아직 멀티플레이가 구현되지 않은 게임도 같은 위치에 준비 중 상태를 표시한다.

## 공통 시작 화면 구조

모든 게임의 첫 설정 화면은 다음 순서를 따른다.

1. 공통 상단바
2. 싱글플레이와 멀티플레이의 2열 설정 영역
3. 랭킹
4. 필요한 경우 게임별 부가 정보

권장 구조:

```text
게임 로고                    도움말  화면 배율  첫 화면
짧은 게임 설명

싱글플레이                   멀티플레이
AI와 바로 시작               친구와 온라인 대전
플레이어 수                  방 만들기
AI 난이도                    방 코드 / 입장
게임별 설정                  접속 상태
게임 시작                    온라인 로비

랭킹
게임별 부가 정보
```

## 상단바

- 왼쪽에는 게임 로고를 배치한다.
- 오른쪽 조작 순서는 `도움말`, `화면 배율`, `첫 화면`으로 통일한다.
- 설정 화면에서는 `새 게임` 버튼을 표시하지 않는다.
- `새 게임`은 실제 플레이 화면에서만 표시한다.
- 아이콘 버튼에는 접근 가능한 이름과 툴팁을 제공한다.
- 로고 크기는 게임마다 달라도 되지만 상단바의 높이와 정렬은 같아야 한다.

## 설정 패널

- 데스크톱에서는 싱글플레이를 왼쪽, 멀티플레이를 오른쪽에 배치한다.
- 두 패널은 같은 너비와 같은 최소 높이를 사용한다.
- 패널 제목, 설명, 입력 영역과 주요 버튼의 세로 위치를 가능한 한 맞춘다.
- 실행 버튼은 패널 하단에 정렬하여 설정 항목 수가 달라도 위치가 흔들리지 않게 한다.
- 닉네임은 첫 화면의 공통 프로필에서 관리하며 게임별 설정 화면에 중복 입력을 만들지 않는다.
- 입력창, 선택 상자와 버튼의 높이, 글자 크기, 여백을 공통 규격으로 사용한다.
- 게임 전용 설정은 싱글플레이 패널의 공통 입력 영역 아래에 추가한다.
- 패널 안에 또 다른 장식용 카드 패널을 중첩하지 않는다.

## 멀티플레이 흐름

모든 게임은 다음 흐름을 기준으로 한다.

```text
게임 선택 -> 시작 설정 -> 방 만들기 또는 입장 -> 온라인 로비 -> 게임 시작
```

- 시작 설정 화면에는 방 생성과 입장에 필요한 최소 정보만 표시한다.
- 채팅, 참가자 목록, 방장 표시와 준비 상태는 온라인 로비에서 표시한다.
- 시작 화면에 큰 채팅 영역을 두지 않는다.
- 멀티플레이 미구현 상태에서는 패널을 제거하지 말고 `준비 중` 상태로 유지한다.

## 공통 요소와 게임별 요소

공통으로 유지할 항목:

- 화면 구조와 반응형 전환 기준
- 상단바 조작 순서
- 설정 패널 크기와 간격
- 폼 컨트롤과 주요 버튼 규격
- 첫 화면 공통 프로필 연동 방식
- 로딩창의 구조와 동작
- 랭킹 목록의 기본 구조
- 화면 배율 조절 방식

게임별로 변경할 수 있는 항목:

- 게임 로고와 배경
- 강조색과 패널 표면색
- 게임 전용 이미지
- 플레이어 수 범위와 AI 난이도 설명
- 확장팩 등 게임 전용 설정
- 랭킹 분류와 게임별 기록 정보

## 랭킹

- 모든 게임의 시작 설정 아래에는 전체 너비의 랭킹 영역을 둔다.
- 랭킹 제목, 새로고침 버튼, 순위 표식, 닉네임, 기록값과 보조 정보의 위치를 공통으로 유지한다.
- 데스크톱에서는 기록 분류를 2열로 표시하고 모바일에서는 1열로 전환한다.
- 기록 기준은 게임 특성에 맞춘다. 예: 점수, 최단 턴, 연승, 최종 자산 또는 팀별 기록.
- 실제 데이터가 없는 상태에서는 빈 상태를 표시하며, 운영 화면에 시안용 가짜 기록을 남기지 않는다.

## 게임별 시각 방향

- 판타지왕국: 갈색과 금색의 판타지 테이블 분위기
- 클루: 검정과 빨강의 추리극 분위기
- 캔트스탑: 남색과 노랑의 경쾌한 주사위 게임 분위기
- 탤리호: 검정, 빨강과 숲색의 사냥 게임 분위기
- 부루마불: 밝은 아이보리와 파랑의 여행 보드게임 분위기

게임별 색은 달라도 텍스트 대비, 조작 크기와 정보 위계는 같아야 한다.

## 반응형 기준

- 넓은 화면에서는 설정 패널을 2열로 표시한다.
- 좁은 화면에서는 싱글플레이 다음에 멀티플레이가 나오도록 1열로 전환한다.
- 모바일에서도 상단바 조작이 겹치거나 화면 밖으로 나가지 않아야 한다.
- 긴 게임 로고는 컨테이너 안에서 비율을 유지하고 잘리지 않아야 한다.
- 버튼과 입력창의 텍스트는 확대 또는 축소 시에도 잘리면 안 된다.
- iPhone과 iPad Safari에서도 Pretendard 웹폰트와 UI 배율이 동일하게 동작해야 한다.

## 구현 규칙

- 공통 시작 화면 스타일은 가능한 한 공유 CSS에 둔다.
- 게임별 CSS는 공통 구조를 다시 작성하지 말고 색상과 게임 전용 요소만 덮어쓴다.
- 공통 색상, 간격, 반경과 컨트롤 높이는 CSS 사용자 정의 속성으로 관리한다.
- 새 게임 페이지는 기존 공통 클래스와 공유 스크립트를 우선 사용한다.
- 기존 게임의 고유 플레이 화면은 시작 화면 통일 작업 때문에 불필요하게 변경하지 않는다.
- 화면을 수정한 뒤 데스크톱과 모바일 크기에서 시작 화면을 직접 확인한다.

## 완료 확인

시작 화면 작업을 완료하기 전에 다음을 확인한다.

- 로고와 오른쪽 상단 조작이 다른 게임과 같은 위치에 있는가
- 싱글플레이와 멀티플레이 패널의 크기와 정렬이 맞는가
- 주요 실행 버튼이 같은 높이와 위치에 있는가
- 게임별 닉네임 입력을 추가하지 않고 첫 화면 공통 프로필을 사용하는가
- 채팅과 참가자 목록이 시작 화면이 아니라 온라인 로비에 있는가
- 게임별 색상과 로고가 유지되는가
- 데스크톱, 모바일과 Safari에서 텍스트가 겹치거나 잘리지 않는가
