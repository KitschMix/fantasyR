# 게임 시작 화면 공통화 작업 가이드

이 문서는 `fantasyR`의 게임 시작 화면을 단계적으로 공통화하는 작업을 다른 AI 또는 개발자가 안전하게 이어받기 위한 기준이다.

## 목표

7개 게임의 시작 화면에 같은 UI 뼈대와 디자인 규칙을 적용한다. 게임별 개성, 옵션, 기존 게임 로직은 유지한다.

공통화 대상은 다음과 같다.

- 화면 최대 폭, 바깥 여백, 패널 간격
- 로고/제목 영역과 뒤로가기 버튼의 배치
- 설정 패널, 버튼, 입력 필드, 설명 텍스트
- 랭킹 패널의 헤더, 목록, 빈 상태, 로딩 상태, 오류 상태
- 데스크톱/모바일 반응형 전환 규칙

게임별로 남겨야 하는 요소는 다음과 같다.

- 로고, 대표 색상, 게임 설명
- 플레이어 수와 게임별 설정 옵션
- 랭킹의 종류 및 표시 정보
- 공통 규격으로 해결할 수 없는 최소한의 예외 스타일

## 핵심 원칙

**틀은 하나, 내용과 색은 게임별로 관리한다.**

공통 UI를 바꿀 때는 한 곳의 공통 CSS 또는 공통 구조만 변경해 7개 게임에 반영될 수 있게 한다. 각 게임 파일에 같은 CSS나 마크업을 복사해 넣지 않는다.

권장 구조:

```text
공통 시작 화면 규격
├─ setup-shell: 폭, 여백, 반응형
├─ setup-header: 로고/제목, 뒤로가기
├─ setup-options: 게임별 옵션 영역
├─ setup-ranking: 랭킹 UI
└─ 게임별 설정
   ├─ 로고와 대표 색상
   ├─ 옵션 마크업
   ├─ 랭킹 표시 데이터
   └─ 꼭 필요한 예외 스타일
```

CSS는 공통 토큰을 기준으로 작성한다.

```css
.game-setup {
  --setup-accent: #d4a853;
  --setup-max-width: 1120px;
  --setup-panel-radius: 20px;
  --setup-gap: 24px;
  --setup-logo-height: 72px;
}

.game-setup--monopoly {
  --setup-accent: #2f78c4;
}
```

## 현재 부루마불 랭킹 구현

부루마불은 공통화 시범 화면으로 사용한다. 이미 4인 최단 턴 랭킹 UI와 연동 로직이 구현되어 있다.

- `monopoly.html`: 시작 화면 및 랭킹 패널
- `monopoly.js`: 게임 종료 후 랭킹 기록 제출 (`turns` 컬럼 포함)
- `setup-shell.js` + `scripts/player-stats.js`: `data-live-ranking` + `data-ranking-mode="turns"` 자동 부착 / `fetchTopRankingsByTurns()` 호출
- `supabase-monopoly-turns.sql`: `fantasy_player_stats.turns` 컬럼 + `fantasy_player_stats_turns_idx` 인덱스
- `supabase-player-stats.sql` (또는 `supabase-schema.sql` 통합본): `fantasy_player_stats` 메인 테이블

> **참고 (구버전)**: 초기 구현은 `shared-fastest-turn-rankings.js` + `fantasy_monopoly_leaderboard` 별도 테이블을 사용했으나 (`2e897c3`), 후속 리팩토링(`263f2d1`/`44d5c0a`/`de03bc8`)에서 **`fantasy_player_stats` + `turns` 컬럼으로 통합**되었다. `.fastest-ranking-*` CSS 클래스 및 `codex/fastest-turn-rankings` 브랜치는 통합 작업으로 제거됨.

## 반드시 지킬 안전 규칙

1. **수정 전 사용자 확인 필수**

   파일을 수정하거나 공통화를 시작하기 전에 반드시 아래처럼 사용자에게 물어본다.

   > 시작 화면 공통화를 진행하려고 합니다. 먼저 부루마불을 기준 화면으로 공통 구조에 옮기고, 데스크톱·모바일 검증 후 나머지 게임으로 확장하겠습니다. 지금 코드를 수정해도 될까요?

   사용자가 명확히 승인하기 전에는 파일을 수정하지 않는다. 분석, 파일 목록 확인, Git 이력 확인, 변경 계획 작성만 허용된다.

2. 게임 로직과 데이터 연동을 바꾸지 않는다.

   특히 기존 DOM ID, JavaScript 이벤트 연결, 게임 시작 흐름, 랭킹 제출 로직, Supabase SQL을 UI 정리 과정에서 변경하지 않는다. 구조 변경으로 ID 수정이 불가피하면, 해당 ID 사용처를 먼저 조사하고 영향 범위 및 대안을 사용자에게 보고한 뒤 다시 승인을 받는다.

3. 기존 작업을 덮어쓰지 않는다.

   작업 시작 전 `git status`를 확인한다. 사용자 또는 다른 AI의 변경이 있으면 보존하고, 겹치는 파일은 차이를 설명한 뒤 진행 여부를 확인한다.

4. 전역 예외 CSS를 늘리지 않는다.

   `!important`, 광범위한 태그 선택자, 특정 게임을 겨냥한 전역 오버라이드는 피한다. 공통 클래스와 CSS 변수, 범위가 제한된 게임별 modifier를 사용한다.

## 권장 작업 순서

1. 7개 시작 화면의 HTML/CSS를 비교해 공통 요소와 게임별 요소를 표로 정리한다.
2. 공통 클래스, CSS 토큰, 반응형 규칙을 설계한다.
3. 부루마불에만 시범 적용한다.
4. 부루마불의 데스크톱/모바일 화면 및 시작·랭킹 동작을 확인한다.
5. 사용자 승인 후 같은 구조를 나머지 게임에 순차 적용한다.
6. 같은 뷰포트에서 7개 화면을 비교하고, 공통 UI가 일관된지 확인한다.

## 완료 기준

- 공통 패널/버튼/여백/랭킹 UI 변경은 한 곳의 공통 규칙 수정으로 반영된다.
- 게임별 색상과 옵션은 유지된다.
- 모든 게임의 기존 시작, 뒤로가기, 랭킹 관련 동작이 유지된다.
- 데스크톱과 모바일에서 패널이 겹치거나 잘리지 않는다.
- 변경한 파일, 확인한 화면 크기, 남은 게임별 예외를 작업 결과에 명시한다.
